---
checks:
- code: IAM001
  description: Custom check to ensure the Huawei cloud provider is not using share config
  impact: Using shared config leads to credential leaking
  resolution: use assume role
  requiredTypes:
  - terraform
  requiredLabels:
  - huaweicloud
  severity: ERROR
  matchSpec:
    name: shared_config_file
    action: isPresent
  errorMessage: Using share config file is not permitted
  relatedLinks:
  - http://internal.acmecorp.com/standards/aws/tagging.html
